#pragma once
#include <string>

namespace add_mode{
    int add(int a, int b);
    double add(double a, double b);
    std::string add(std::string a, std::string b);
    void run(int argc, char* argv[]);
}
